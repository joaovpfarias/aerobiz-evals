# ROMs (nao versionadas)

Coloque aqui o **seu dump** de Aerobiz Supersonic (USA) para SNES:

```
experiments/aerobiz_evals/roms/Aerobiz Supersonic (USA).sfc
```

- O `.gitignore` bloqueia commit de `*.sfc`/`*.smc` — mantenha assim (copyright Koei Tecmo).
- O harness nao baixa ROMs; a ROM precisa ser fornecida pelo dono do cartucho.
- Homebrew de teste (legal, redistribuivel): `HelloWorld.sfc` de PeterLemon/SNES — usada so para smoke-test da ponte.
